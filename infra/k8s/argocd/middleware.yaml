apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argocd-secure-headers
  namespace: argocd
spec:
  headers:
    frameDeny: true
    contentTypeNosniff: true
    browserXssFilter: true
    stsIncludeSubdomains: true
    stsPreload: true
    stsSeconds: 31536000