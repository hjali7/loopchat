auth:
  enabled: true
  rootPassword: "root-password-change-me"
  usernames: ["loopchat"]
  databases: ["loopchat_db"]
  passwords: ["loopchat-password-change-me"]

architecture: standalone

persistence:
  enabled: true
  storageClass: "local-path"
  size: 2Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi